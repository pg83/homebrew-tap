# pg83/tap

```sh
brew install pg83/tap/shitty
```

Binary releases of [shitty](https://github.com/pg83/shitty), the fastest
terminal emulator on Earth. The formula is regenerated automatically: a
release in the shitty repository sends a `repository_dispatch` here and
the update workflow rewrites `Formula/shitty.rb` against the new asset.
